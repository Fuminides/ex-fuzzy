name: Tests

on:
  push:
    branches: [ main, evox-support, Conformal ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov
        pip install -e .

    - name: Run tests with pytest
      run: |
        pytest tests/ -v --tb=short --cov=ex_fuzzy --cov-report=xml --cov-report=term-missing

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      if: matrix.python-version == '3.12'
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  test-minimal:
    runs-on: ubuntu-latest
    name: Test (minimal deps)
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install minimal dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        pip install numpy pandas scikit-learn matplotlib pymoo

    - name: Install package
      run: |
        pip install -e .

    - name: Run core tests
      run: |
        pytest tests/test_fuzzy_sets_comprehensive.py tests/test_rules_comprehensive.py -v --tb=short

  test-evox:
    runs-on: ubuntu-latest
    name: Test (EvoX backend, CPU)
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install the EvoX backend with CPU-only PyTorch
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        pip install torch --index-url https://download.pytorch.org/whl/cpu
        pip install -e ".[evox]"

    - name: Run the backend tests
      run: |
        pytest tests/test_evox_population.py tests/test_evolutionary_backends.py tests/test_optional_pymoo.py tests/test_evolutionary_fit_regression.py -v --tb=short
